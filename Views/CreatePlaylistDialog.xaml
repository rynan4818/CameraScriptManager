<Window x:Class="CameraScriptManager.Views.CreatePlaylistDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="プレイリスト作成" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="タイトル:" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <!-- Author -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="作者:" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Author, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <!-- Description -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="説明:" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

        <!-- Cover Image -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="カバー画像:" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CoverImagePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,8" IsReadOnly="True" />
        <Button Grid.Row="3" Grid.Column="2" Content="参照..." Width="50" Margin="0,0,0,8" Click="BrowseImage_Click"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="作成" Width="80" Margin="0,0,8,0" Click="Create_Click" IsDefault="True"/>
            <Button Content="キャンセル" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
